{
  "name": "prisma-cursor-plugin",
  "version": "0.1.0",
  "description": "Cursor plugin for Prisma: MCP server, rules, skills, and code generation",
  "private": true,
  "workspaces": [
    "mcp-server"
  ],
  "scripts": {
    "build": "npm run build --workspace=mcp-server",
    "dev": "npm run dev --workspace=mcp-server"
  },
  "engines": {
    "node": ">=18"
  }
}
